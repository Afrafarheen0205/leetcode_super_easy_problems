class Solution(object):
    def sortedSquares(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        k=len(nums)
        for i in range(0,k):
            nums[i]=nums[i]*nums[i]
        nums.sort()
        return nums
